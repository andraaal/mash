# mash
## Setup
If you want to download this it is really not difficult. You just need cargo and a somewhat recent rust version and your good to go. Just run `cargo build` and there is your mash shell.
## Features
Current features include arbitrary command execution with arguments, builtin (exit, echo, type, pwd and cd), and piping which is currently bugged. That's it currently.
